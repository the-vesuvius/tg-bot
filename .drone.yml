kind: pipeline
name: default
load: secrets
steps:
  - name: docker
    image: plugins/docker
    settings:
      username: vesuviusua
      password:
        from_secret: docker_pass
      repo: vesuviusua/tg-bot
      tags:
        - latest
#      host: 64.225.102.134
#      insecure: true
#      daemon_off: true
#      ssh_key:
#        from_secret: ssh_private_key
#      deploy_key:
#        from_secret: ssh_private_key